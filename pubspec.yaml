name: armory_last
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.13.2

dependencies:
  flutter:
    sdk: flutter

  flutter_web_plugins:
    sdk: flutter
  provider: ^6.1.5+1
  go_router: ^18.0.1
  shared_preferences: ^2.5.3
  pocketbase: ^0.25.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  http: ^1.6.0

  flutter_lints: ^6.0.0

flutter:

  uses-material-design: true

